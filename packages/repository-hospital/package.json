{
  "name": "@medevac/repo-hospital",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "keywords": [],
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pnpm prisma:generate &&  tsup",
    "build:packages": "pnpm prisma:generate && pnpm run build",
    "prisma:generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "6.5.0"
  },
  "devDependencies": {
    "prisma": "*",
    "tsup": "*",
    "typescript": "*"
  }
}
