version: "3.8"
services:
  auth:
    image: "devpala/medevac-auth:latest"
    ports:
      - 3000:3000
    environment:
      - DATABASE_URL=postgresql://medevac-auth-user:medevac-auth-password@database:5432/medevac-auth-database?schema=public
      - SECRET_TOKEN=${SECRET_TOKEN}
      - PORT=3000
    networks:
      - medevac-networks
  database-auth:
    image: postgres:16-alpine
    ports:
      - 5432:5432
    volumes:
      - ./.database/auth:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=medevac-auth-password
      - POSTGRES_USER=medevac-auth-user
      - POSTGRES_DB=medevac-auth-database
    networks:
      - medevac-networks
  migrate-database:
    image: "devpala/medevac-auth-migration-database:latest"
    depends_on:
      - database-auth
    environment:
      - DATABASE_URL=postgresql://medevac-auth-user:medevac-auth-password@database-auth:5432/medevac-auth-database?schema=public
    command: bunx prisma migrate deploy
    networks:
      - medevac-networks
  mission:
    image: "devpala/medevac-auth:latest"
    ports:
      - 3001:3000
    environment:
      - DATABASE_URL=postgresql://medevac-auth-user:medevac-auth-password@database:5432/medevac-mission-database?schema=public
      - SECRET_TOKEN=${SECRET_TOKEN}
      - PORT=3001
  database-mission:
    image: postgres:16-alpine
    ports:
      - 5432:5432
    volumes:
      - ./.database/mission:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=medevac-auth-password
      - POSTGRES_USER=medevac-auth-user
      - POSTGRES_DB=medevac-auth-database
    networks:
      - medevac-networks
networks:
  medevac-networks:
    external: true
